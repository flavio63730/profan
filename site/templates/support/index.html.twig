{% extends 'base.html.twig' %}

{% block title %}Supports{% endblock %}

{% block body %}
    <div class="box">
        <div class="box-header">
            <h1 class="box-title">{{ 'Liste des supports'|trans }}</h1>
            <a href="{{ url('app_support_new') }}" class="btn btn-primary top-btn">{{ 'Créer un support'|trans }}</a>
        </div>
        <div class="box-body">
            <table class="table table-striped dataTable" role="grid">
                <thead>
                    <tr role="row">
                        <th>Article</th>
                        <th>Designation</th>
                        <th>Prenom</th>
                        <th>Quantite</th>
                        <th>Couleur</th>
                        <th>Format</th>
                        <th>Grammage</th>
                        <th>Materiel</th>
                        <th>Type<th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                {% for support in supports %}
                    <tr role="row">
                        <td>{{ support.id }}</td>
                        <td>{{ support.desciption }}</td>
                        <td>{{ support.quantite }}</td>
                        <td>{{ support.couleur }}</td>
                        <td>{{ support.format }}</td>
                        <td>{{ support.grammage }}</td>
                        <td>{{ support.materiel }}</td>
                        <td>{{ support.type }}</td>
                        <td class="actions_form">
                            <a href="{{ path('app_support_edit', { 'id': support.id}) }}" class="btn btn-default">
                                <i class="fa fa-pencil"></i>
                            </a>
                            {{ include('support/_delete_form.html.twig') }}
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
